[build]
  command = "npm run build"
  publish = "dist"
  ignore = "exit 0"

[dev]
  command = "npm run start"
  targetPort = 8085

[[edge_functions]]
  function = "eleventy-edge"
  path = "/*"

[functions]
  node_bundler = "esbuild"
