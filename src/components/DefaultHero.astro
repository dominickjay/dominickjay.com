---
const { title, description, growth, lastModified, updatedDate } = Astro.props;
const date = lastModified ?? updatedDate;
const formattedDate =
  date &&
  date.toLocaleDateString("en-us", {
    year: "numeric",
    month: "short",
    day: "numeric",
  });
---

<header>
  <div
    class="container cluster relative"
    style="--cluster-vertical-alignment: flex-end; --gutter: 32px;"
  >
    <div class="flow" style="--flow-space: var(--space-m)">
      {
        growth && (
          <div class="h-fit h4 flex items-baseline gap-[8px]">
            <span class={`growth`} data-growth={growth}>
              {growth.charAt(0).toUpperCase() + growth.slice(1)}
            </span>
          </div>
        )
      }
      {
        date && (
          <div class="last-updated" data-scramble-on-load>
            <span data-scramble=`Last updated: ${formattedDate}` aria-hidden="true" />
            <span class="sr-only">
              Last updated:
              <time datetime={date.toISOString()}>{formattedDate}</time>
            </span>
          </div>
        )
      }
      <h1
        class="h1 gradient-text md:max-w-[85vw]"
        style="--flow-space: var(--space-l)"
      >
        {title}
      </h1>
      <h2 class="intro">{description}</h2>
    </div>
  </div>
</header>

<script is:inline>
		function updateTimeTheme() {
	    const hour = typeof Temporal !== "undefined"
	      ? Temporal.Now.zonedDateTimeISO().hour
	      : new Date().getHours();

	    let period = "";
	    if (hour >= 5 && hour < 9) period = "early-morning";
	    else if (hour >= 9 && hour < 12) period = "late-morning";
	    else if (hour >= 12 && hour < 15) period = "early-afternoon";
	    else if (hour >= 15 && hour < 17) period = "mid-afternoon";
	    else if (hour >= 17 && hour < 19) period = "last-afternoon";
	    else if (hour >= 19 && hour < 22) period = "evening";
	    else period = "night";

	    document.documentElement.setAttribute("data-time", period);
	  }

  updateTimeTheme();
</script>
